/*
 *
 * Write a method to replace all spaces in a string with "%20"
 */

namespace LiveCodingTasks
{
    public static class RemoveSpacesFromString
    {
        public static char[] ReplaceFun(char[] str, int length)
        {
            // Dummy incorrect return
            return new char[1] {' '};
        }
    }
}